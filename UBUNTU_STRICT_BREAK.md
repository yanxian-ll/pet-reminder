# Ubuntu 严格休息模式

版本 0.2.1 保持原有桌宠休息页面可见，并在休息倒计时期间阻止普通键盘和鼠标操作。

## 必须使用 Xorg 会话

先检查当前会话：

```bash
echo "$XDG_SESSION_TYPE"
```

结果必须是：

```text
x11
```

如果显示 `wayland`：

1. 注销 Ubuntu。
2. 在登录界面选择你的用户。
3. 点击右下角齿轮。
4. 选择 **Ubuntu on Xorg**。
5. 重新登录。

## 休息期间行为

休息开始时，程序会让当前桌宠休息页面覆盖所有显示器、持续置顶，并获取 Xorg 的全局键盘和鼠标输入。普通点击、输入、Alt+Tab、工作区切换及桌面操作会被阻止，直到倒计时结束。

倒计时结束后，程序自动释放键盘和鼠标，恢复桌宠工作窗口。

该实现不会禁用 `/dev/input` 设备，也不安装特权后台服务。程序进程异常退出时，X 服务器会自动释放输入抓取。
